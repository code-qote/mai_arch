components_manager:
    task_processors:                  # Task processor is an executor for coroutine tasks
        main-task-processor:          # Make a task processor for CPU-bound coroutine tasks.
            worker_threads: 4         # Process tasks in 4 threads.
  
        fs-task-processor:            # A separate task processor for filesystem bound and synchronous tasks.
            worker_threads: 1
  
    components:                       # Configuring components that were registered via component_list
        server:
            listener:                 # configuring the main listening socket...
                port: 8082            # ...to listen on this port (different from user and event services)
        logging:
            loggers:
                default:
                    file_path: '@stderr'
                    level: debug
                    overflow_behavior: discard  # Drop logs if the system is too busy to write them down.
  
        testsuite-support:
            testsuite-periodic-update-enabled: true
            testsuite-pg-execute-timeout: 300s
            testsuite-pg-statement-timeout: 300s
            testsuite-pg-readonly-master-expected: false

        default-secdist-provider:
            config: ./configs/secdist.json

        secdist:
            provider: default-secdist-provider

        # Redis cache connection
        redis-cache-db:
            thread_pools:
                redis_thread_pool_size: 4
                sentinel_thread_pool_size: 1
            groups:
                - config_name: redis-cache  # Key to lookup in secdist configuration
                  db: redis-cache           # Name to refer to the cluster in GetClient()
            subscribe_groups: []

        # PostgreSQL database connection
        postgres-db:
            dbconnection#env: POSTGRES_DSN
            dbconnection#fallback: 'postgresql://eventbooking:eventbooking_password@postgres:5432/eventbooking'
            blocking_task_processor: fs-task-processor
            dns_resolver: async
            sync-start: true
            connlimit_mode: manual

        # DNS client for HTTP client
        dns-client:
            fs-task-processor: fs-task-processor

        # HTTP client for calling other services
        http-client: {}
        http-client-core:
            fs-task-processor: fs-task-processor
        http-client-middleware-pipeline: {}

        jwt-component: {}

        auth-middleware: {}               # JWT authentication middleware factory

        default-server-middleware-pipeline-builder:
            append:
                - auth-middleware

        handler-book-event:               # Event booking handler (requires auth)
            path: /bookings               # Registering handler by URL '/bookings'.
            method: POST                  # It will only reply to POST requests.
            event_service_url: http://event-service:8081  # URL of the event service (Docker network)

        handler-cancel-booking:           # Booking cancellation handler (requires auth)
            path: /bookings/cancel        # Registering handler by URL '/bookings/cancel'.
            method: POST                  # It will only reply to POST requests.
            event_service_url: http://event-service:8081  # URL of the event service (Docker network)
